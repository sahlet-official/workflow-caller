{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Attach to Docker (Node.js)",
            "type": "node",
            "request": "attach",
            "address": "localhost",
            "port": 9229,
            "localRoot": "${workspaceFolder}",
            "remoteRoot": "/app",
            "skipFiles": ["<node_internals>/**"]
        }
    ]
}